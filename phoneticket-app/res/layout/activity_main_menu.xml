<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="top"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainMenuActivity" >

    <ScrollView
        android:id="@+id/scrollView1"
        android:layout_width="match_parent"
        android:layout_height="match_parent" >

        <LinearLayout
            android:id="@+id/linearLayout1"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical" >

            <TextView
                android:id="@+id/welcome_message"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="Bienvenido"
                android:textAppearance="?android:attr/textAppearanceMedium"
                android:visibility="visible" />

            <ImageView
                android:id="@+id/complejosPhoto01"
                android:layout_width="184dp"
                android:layout_height="184dp"
                android:layout_gravity="center"
                android:layout_marginTop="5dp"
                android:src="@drawable/pochoclo" />

            <Button
                android:id="@+id/peliculasButton"
                style="@style/ButtonMenu"
                android:drawableStart="@drawable/peliculas"
                android:text="@string/cartelera" />

            <Button
                android:id="@+id/complejoButton"
                style="@style/ButtonMenu"
                android:drawableStart="@drawable/complejosicon"
                android:text="@string/complejo" />

            <Button
                android:id="@+id/miCuentaButton"
                style="@style/ButtonMenu"
                android:drawableStart="@drawable/micuentaicon"
                android:text="@string/mi_cuenta" />
        </LinearLayout>

    </ScrollView>

</RelativeLayout>